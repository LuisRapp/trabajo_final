<div class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="mb-0">
            <i class="bi bi-gear-fill"></i> Kits de Mantenimiento Preventivo
        </h1>
    </div>

    {{-- Mensajes de feedback --}}
    @if (session()->has('message'))
        <div class="alert alert-success alert-dismissible fade show">
            {{ session('message') }}
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    @endif
    @if (session()->has('error'))
        <div class="alert alert-danger alert-dismissible fade show">
            {{ session('error') }}
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    @endif

    {{-- Pestañas (Tabs) --}}
    <ul class="nav nav-tabs mb-4" id="kitsTabs" role="tablist">
        <li class="nav-item" role="presentation">
            <button class="nav-link active" id="nuevo-kit-tab" data-bs-toggle="tab" data-bs-target="#nuevo-kit" type="button" role="tab">
                <i class="bi bi-plus-circle"></i> {{ $editando_kit ? 'Editar Kit' : 'Nuevo Kit' }}
            </button>
        </li>
        <li class="nav-item" role="presentation">
            <button class="nav-link" id="listado-kits-tab" data-bs-toggle="tab" data-bs-target="#listado-kits" type="button" role="tab">
                <i class="bi bi-list-ul"></i> Listado de Kits ({{ count($kits_registrados) }})
            </button>
        </li>
    </ul>

    <div class="tab-content" id="kitsTabContent">
        {{-- Tab 1: Nuevo/Editar Kit --}}
        <div class="tab-pane fade show active" id="nuevo-kit" role="tabpanel">
            <div class="card shadow">
                <div class="card-header bg-light">
                    <h5 class="mb-0">
                        <i class="bi bi-tools"></i> 
                        @if($tipo_seleccionado)
                            Kit Mantenimiento Preventivo {{ $tipo_seleccionado->nombre }}
                        @else
                            Configuración de Kit
                        @endif
                    </h5>
                </div>
                <div class="card-body">
                    @if($kit_modificado && $tipo_maquinaria_seleccionada)
                        <div class="alert alert-info alert-dismissible fade show">
                            <i class="bi bi-info-circle"></i> 
                            <strong>Cambios pendientes:</strong> Recuerde registrar el kit para confirmar la configuración.
                            <button wire:click="registrarKit" class="btn btn-sm btn-success ms-3">
                                <i class="bi bi-check-circle"></i> Registrar Ahora
                            </button>
                            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                        </div>
                    @endif

                    {{-- Selector de Tipo de Maquinaria --}}
                    <div class="row mb-4">
                        <div class="col-md-8">
                            <label class="form-label fw-bold">Tipo de Maquinaria <span class="text-danger">*</span></label>
                            <select wire:model="tipo_maquinaria_seleccionada" class="form-select" {{ $editando_kit ? 'disabled' : '' }}>
                                <option value="">Seleccionar tipo de maquinaria...</option>
                                @foreach($tipos_maquinaria as $tipo)
                                    <option value="{{ $tipo->id_tipo_maquinaria }}">
                                        {{ $tipo->nombre }}
                                        @if($tipo->umbral_toneladas)
                                            - Umbral: {{ number_format($tipo->umbral_toneladas, 2) }} ton
                                        @endif
                                    </option>
                                @endforeach
                            </select>
                            @if($editando_kit)
                                <small class="text-muted">
                                    <i class="bi bi-info-circle"></i> Editando kit existente. 
                                    <a href="#" wire:click.prevent="limpiarKit">Cancelar edición</a>
                                </small>
                            @endif
                        </div>

                        <div class="col-md-4 d-flex align-items-end gap-2">
                            @if($tipo_maquinaria_seleccionada && count($items_kit) > 0)
                                <button wire:click="registrarKit" class="btn btn-success">
                                    <i class="bi bi-check-circle"></i> 
                                    {{ $editando_kit ? 'Actualizar' : 'Registrar' }}
                                    @if($kit_modificado)
                                        <span class="badge bg-warning text-dark ms-1">*</span>
                                    @endif
                                </button>
                                <button wire:click="limpiarKit" class="btn btn-outline-secondary">
                                    <i class="bi bi-x-circle"></i> Limpiar
                                </button>
                            @endif
                        </div>
                    </div>

                    @if($tipo_seleccionado && !$tipo_seleccionado->umbral_toneladas)
                        <div class="alert alert-warning">
                            <i class="bi bi-exclamation-triangle"></i>
                            Este tipo de maquinaria no tiene umbral de toneladas configurado
                        </div>
                    @endif
                        @foreach($tipos_maquinaria as $tipo)
                            <option value="{{ $tipo->id_tipo_maquinaria }}">
                                {{ $tipo->nombre }}
                                @if($tipo->umbral_toneladas)
                                    - Umbral: {{ number_format($tipo->umbral_toneladas, 2) }} ton
                                @endif
                            </option>
                        @endforeach
                    </select>
                </div>

                <div class="col-md-6 d-flex align-items-end gap-2">
                    @if($tipo_maquinaria_seleccionada && count($items_kit) > 0)
                        <button wire:click="registrarKit" class="btn btn-success">
                            <i class="bi bi-check-circle"></i> Registrar Kit
                            @if($kit_modificado)
                                <span class="badge bg-warning text-dark ms-1">*</span>
                            @endif
                        </button>
                        <button wire:click="limpiarKit" class="btn btn-outline-secondary">
                            <i class="bi bi-arrow-clockwise"></i> Nuevo Kit
                        </button>
                    @endif
                    
                    @if($tipo_seleccionado && !$tipo_seleccionado->umbral_toneladas)
                        <div class="alert alert-warning mb-0 flex-grow-1">
                            <i class="bi bi-exclamation-triangle"></i>
                            Sin umbral de toneladas configurado
                        </div>
                    @endif
                </div>
            </div>

            @if($tipo_maquinaria_seleccionada)
                {{-- Tabs --}}
                <ul class="nav nav-tabs mb-3" role="tablist">
                    <li class="nav-item" role="presentation">
                        <button class="nav-link active" data-bs-toggle="tab" data-bs-target="#tab-actual" type="button" role="tab">
                            <i class="bi bi-list-check"></i> Kit Actual
                        </button>
                    </li>
                    <li class="nav-item" role="presentation">
                        <button class="nav-link" data-bs-toggle="tab" data-bs-target="#tab-historial" type="button" role="tab">
                            <i class="bi bi-archive"></i> Historial de Bajas
                        </button>
                    </li>
                </ul>

                <div class="tab-content">
                    {{-- Tab: Kit Actual --}}
                    <div class="tab-pane fade show active" id="tab-actual" role="tabpanel">
                        {{-- Barra de acciones --}}
                        <div class="d-flex justify-content-between align-items-center mb-3">
                            <div>
                                <small class="text-muted">
                                    <i class="bi bi-box-seam"></i> {{ count($items_kit) }} insumo(s) configurado(s)
                                </small>
                            </div>
                            <button wire:click="abrirModalAgregar" class="btn btn-primary btn-sm">
                                <i class="bi bi-plus-circle"></i> Agregar Insumo
                            </button>
                        </div>

                        {{-- Tabla de Items del Kit --}}
                        <div class="table-responsive">
                            <table class="table table-hover align-middle">
                                <thead class="table-light">
                                    <tr>
                                        <th width="8%">ID</th>
                                        <th width="30%">Insumo</th>
                                        <th width="15%">Cant. Requerida</th>
                                        <th width="15%">Stock Actual</th>
                                        <th width="15%">Tipo</th>
                                        <th width="17%" class="text-end">Acciones</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @forelse($items_kit as $item)
                                        <tr>
                                            <td><span class="badge bg-secondary">{{ $item->id_kit }}</span></td>
                                            <td>
                                                <strong>{{ $item->insumo->nombre }}</strong>
                                            </td>
                                            <td>{{ number_format($item->cantidad_requerida, 2) }}</td>
                                            <td>
                                                @php
                                                    $stock = $item->insumo->stock;
                                                    $suficiente = $stock >= $item->cantidad_requerida;
                                                @endphp
                                                <span class="badge bg-{{ $suficiente ? 'success' : 'danger' }}">
                                                    {{ number_format($stock, 2) }}
                                                </span>
                                                @if(!$suficiente)
                                                    <br><small class="text-danger">Faltan {{ number_format($item->cantidad_requerida - $stock, 2) }}</small>
                                                @endif
                                            </td>
                                            <td>
                                                @if($item->es_obligatorio)
                                                    <span class="badge bg-danger"><i class="bi bi-exclamation-circle"></i> Obligatorio</span>
                                                @else
                                                    <span class="badge bg-secondary"><i class="bi bi-info-circle"></i> Opcional</span>
                                                @endif
                                            </td>
                                            <td class="text-end">
                                                <div class="btn-group btn-group-sm">
                                                    <button wire:click="abrirModalEditar({{ $item->id_kit }})" 
                                                            class="btn btn-outline-warning" 
                                                            title="Editar">
                                                        <i class="bi bi-pencil"></i>
                                                    </button>
                                                    <button wire:click="eliminar({{ $item->id_kit }})" 
                                                            class="btn btn-outline-danger" 
                                                            onclick="return confirm('¿Está seguro de dar de baja este insumo del kit?')" 
                                                            title="Dar de baja">
                                                        <i class="bi bi-trash"></i>
                                                    </button>
                                                </div>
                                            </td>
                                        </tr>
                                    @empty
                                        <tr>
                                            <td colspan="6" class="text-center text-muted py-5">
                                                <i class="bi bi-inbox display-4 d-block mb-3"></i>
                                                <p>No hay insumos configurados para este kit</p>
                                                <button wire:click="abrirModalAgregar" class="btn btn-primary btn-sm">
                                                    <i class="bi bi-plus-circle"></i> Agregar Primer Insumo
                                                </button>
                                            </td>
                                        </tr>
                                    @endforelse
                                </tbody>
                            </table>
                        </div>

                        {{-- Resumen y Acciones --}}
                        @if(count($items_kit) > 0)
                            <div class="row mt-3 g-3">
                                <div class="col-md-8">
                                    <div class="alert alert-light border">
                                        <div class="row text-center">
                                            <div class="col-3">
                                                <strong class="d-block text-muted small">Total Insumos</strong>
                                                <span class="h5 mb-0">{{ count($items_kit) }}</span>
                                            </div>
                                            <div class="col-3">
                                                <strong class="d-block text-muted small">Obligatorios</strong>
                                                <span class="h5 mb-0 text-danger">{{ $items_kit->where('es_obligatorio', true)->count() }}</span>
                                            </div>
                                            <div class="col-3">
                                                <strong class="d-block text-muted small">Opcionales</strong>
                                                <span class="h5 mb-0 text-secondary">{{ $items_kit->where('es_obligatorio', false)->count() }}</span>
                                            </div>
                                            <div class="col-3">
                                                @php
                                                    $conStock = $items_kit->filter(fn($it) => $it->insumo->stock >= $it->cantidad_requerida)->count();
                                                    $todosConStock = $conStock === count($items_kit);
                                                @endphp
                                                <strong class="d-block text-muted small">Stock OK</strong>
                                                <span class="h5 mb-0 badge bg-{{ $todosConStock ? 'success' : 'warning' }}">
                                                    {{ $conStock }}/{{ count($items_kit) }}
                                                </span>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                                <div class="col-md-4 d-flex align-items-center justify-content-end">
                                    <div class="btn-group">
                                        <button wire:click="registrarKit" class="btn btn-success">
                                            <i class="bi bi-check-circle-fill"></i> 
                                            {{ $kit_modificado ? 'Guardar Cambios' : 'Confirmar Kit' }}
                                        </button>
                                        <button wire:click="limpiarKit" class="btn btn-outline-secondary" 
                                                title="Configurar otro kit">
                                            <i class="bi bi-plus-square"></i> Nuevo
                                        </button>
                                    </div>
                                </div>
                            </div>
                        @endif
                    </div>

                    {{-- Tab: Historial --}}
                    <div class="tab-pane fade" id="tab-historial" role="tabpanel">
                        @php($historial = $this->cargarHistorial())
                        <div class="table-responsive">
                            <table class="table table-striped align-middle">
                                <thead class="table-light">
                                    <tr>
                                        <th width="8%">ID</th>
                                        <th width="35%">Insumo</th>
                                        <th width="15%">Cant. Requerida</th>
                                        <th width="20%">Fecha de Baja</th>
                                        <th width="22%" class="text-end">Acciones</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @forelse($historial as $item)
                                        <tr class="table-warning">
                                            <td><span class="badge bg-secondary">{{ $item->id_kit }}</span></td>
                                            <td>
                                                <strong>{{ optional($item->insumo)->nombre ?? '—' }}</strong>
                                                @if($item->es_obligatorio)
                                                    <span class="badge bg-danger ms-2">Obligatorio</span>
                                                @endif
                                            </td>
                                            <td>{{ number_format($item->cantidad_requerida, 2) }}</td>
                                            <td>
                                                <i class="bi bi-clock-history text-muted"></i>
                                                <small>{{ optional($item->deleted_at)->format('d/m/Y H:i') ?? '—' }}</small>
                                            </td>
                                            <td class="text-end">
                                                <button wire:click="restaurar({{ $item->id_kit }})" 
                                                        class="btn btn-success btn-sm" 
                                                        title="Restaurar item">
                                                    <i class="bi bi-arrow-counterclockwise"></i> Restaurar
                                                </button>
                                            </td>
                                        </tr>
                                    @empty
                                        <tr>
                                            <td colspan="5" class="text-center text-muted py-5">
                                                <i class="bi bi-archive display-4 d-block mb-3"></i>
                                                <p>No hay bajas registradas para este kit</p>
                                            </td>
                                        </tr>
                                    @endforelse
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            @else
                <div class="alert alert-info text-center py-5">
                    <i class="bi bi-arrow-up-circle display-4"></i>
                    <p class="mt-3 mb-0">Seleccione un tipo de maquinaria para configurar su kit de mantenimiento</p>
                </div>
            @endif
        </div>
    </div>
    
    {{-- Modal Agregar/Editar Item --}}
    @if($modal_item)
    <div class="modal fade show" style="display: block; background: rgba(0,0,0,0.5);" tabindex="-1">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header bg-primary text-white">
                    <h5 class="modal-title">
                        {{ $item_id ? 'Editar' : 'Agregar' }} Insumo al Kit
                    </h5>
                    <button type="button" class="btn-close btn-close-white" wire:click="cerrarModal"></button>
                </div>
                <div class="modal-body">
                    <form wire:submit.prevent="guardar">
                        <div class="mb-3">
                            <label class="form-label">Insumo <span class="text-danger">*</span></label>
                            <select wire:model="insumo_id" class="form-select">
                                <option value="">Seleccionar insumo...</option>
                                @foreach($insumos as $insumo)
                                    <option value="{{ $insumo->id_insumo }}">
                                        {{ $insumo->nombre }} 
                                        (Stock: {{ number_format($insumo->stock, 2) }})
                                    </option>
                                @endforeach
                            </select>
                            @error('insumo_id')
                                <span class="text-danger small">{{ $message }}</span>
                            @enderror
                        </div>
                        
                        <div class="mb-3">
                            <label class="form-label">Cantidad Requerida <span class="text-danger">*</span></label>
                            <input type="number" 
                                   wire:model="cantidad_requerida" 
                                   class="form-control" 
                                   step="0.1" min="0" 
                                   min="0.01"
                                   placeholder="Ej: 10.00">
                            @error('cantidad_requerida')
                                <span class="text-danger small">{{ $message }}</span>
                            @enderror
                        </div>
                        
                        <div class="mb-3">
                            <div class="form-check form-switch">
                                <input type="checkbox" 
                                       wire:model="es_obligatorio" 
                                       class="form-check-input" 
                                       id="esObligatorioCheck">
                                <label class="form-check-label" for="esObligatorioCheck">
                                    ¿Es obligatorio?
                                </label>
                            </div>
                            <small class="text-muted">
                                Los insumos obligatorios deben estar disponibles para aprobar el mantenimiento
                            </small>
                        </div>
                    </form>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" wire:click="cerrarModal">
                        Cancelar
                    </button>
                    <button type="button" class="btn btn-primary" wire:click="guardar">
                        <i class="bi bi-save"></i> {{ $item_id ? 'Actualizar' : 'Agregar' }}
                    </button>
                </div>
            </div>
        </div>
    </div>
    @endif
    
</div>
